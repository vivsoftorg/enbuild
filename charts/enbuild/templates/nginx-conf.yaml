apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-nginx-conf
  labels:
{{- include "enbuild-helm-chart.labels" . | nindent 4 }}
data:
  override.conf: |-
    # blank override.conf
    # Intentionally left empty


  default.conf: |-
    # '' -> '' (not 'close'): when the request is not a WebSocket upgrade the
    # Connection header is dropped entirely, which keeps HTTP/1.1 upstream
    # keepalive viable for the plain API traffic that shares these locations.
    map $http_upgrade $connection_upgrade {
      default upgrade;
      '' '';
    }

    # Keep a small pool of warm connections to the backend so every API call
    # doesn't pay TCP (+ Istio sidecar) connection setup. Requires
    # proxy_http_version 1.1 + the $connection_upgrade map above in the
    # locations that use it.
    upstream enbuild_backend {
      server {{ .Release.Name }}-enbuild-backend:80;
      keepalive 16;
    }

    server {
      listen 8080 default_server;
      # absolute_redirect off — keep nginx's trailing-slash / directory 301s RELATIVE.
      # WHY: nginx listens on 8080, but kubectl port-forward reaches it on a DIFFERENT
      # local port (e.g. 8081). With the default absolute_redirect ON, the auto 301 for
      # a slash-less path (e.g. the OIDC callback `/p1-ccm-console?scope=...`) rebuilds
      # the URL using nginx's LISTEN port 8080 -> bounces the browser to localhost:8080,
      # which isn't served/whitelisted, breaking login. Relative 301s stay on whatever
      # host:port the browser used. (Root-caused on vendor13-ib 2026-07-02; the frontend
      # derives its OIDC redirect_uri from window.location.href, so a wrong-port bounce
      # here poisons the redirect_uri too.) Harmless behind the Istio gateway.
      absolute_redirect off;
      # WHY "localhost" is in server_name (DO NOT REMOVE):
      # The Iron Bank (and stock) nginx base image ships a default server block
      # in /etc/nginx/nginx.conf with `server_name localhost`, also listening on
      # 8080. nginx selects server blocks by Host header before falling back to
      # default_server, so any request with Host: localhost (i.e. every kubectl
      # port-forward) matches the base block — which has NO /enbuild-bk/,
      # /enbuild-user/, /headlamp/ location entries. The UI loads but every
      # API call returns the React 404 HTML, the frontend tries to parse that
      # as JSON, and crashes with "Unexpected token '<'". First observed on
      # vendor13-ib (2026-04-20). Adding "localhost" here lets this block tie
      # the base block on hostname match and win on `default_server`.
      # Production access via Istio gateway uses a non-localhost Host header
      # and is unaffected by this line.
      server_name localhost _;

      include override.conf;

      # P1 CCM latency P0 (2026-06-11): the hub is reached through an SSM
      # tunnel + port-forward capped at ~650KB/s — compressing text payloads
      # (825KB /stacks JSON → ~10x smaller) is the single biggest win. The
      # IronBank nginx base has the gzip module compiled in (config-only
      # change). text/html is gzipped by default; do not add it to gzip_types
      # (nginx warns on the duplicate).
      gzip on;
      gzip_comp_level 5;
      gzip_min_length 1024;
      gzip_vary on;
      gzip_proxied any;
      gzip_types application/javascript text/javascript text/css application/json image/svg+xml application/x-ndjson;
      # Long-lived browser connections over the tunnel — don't make the SPA
      # re-handshake every 75s (nginx default).
      keepalive_timeout 300;

      proxy_pass_header Authorization;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;

      location /enbuild-bk/ {
        proxy_pass http://enbuild_backend/;
        # WebSocket-capable + upstream keepalive. NOTE nginx semantics:
        # proxy_set_header at location level REPLACES ALL inherited
        # server-level proxy_set_header directives, so the four server-level
        # headers are replicated here verbatim — do not remove them.
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
      }

      location /enbuild-user/ {
        proxy_pass http://{{ .Release.Name }}-enbuild-user/;
      }

{{- if .Values.lightning_features.operations_lightning.headlamp }}
      # ENBUILD Headlamp white-label (folded into the chart so helm-upgrade
      # doesn't wipe the operator-applied patch — same carryover discipline
      # as the /p1-ccm-console/ block below; see OPERATIONS_GOTCHAS §8/§9).
      # Headlamp's plugin SDK is bridged and CANNOT touch <head>/title/favicon
      # from plugin code (§8) — so the brand erasure + sidebar→top-nav overlay
      # is injected here at the FE nginx layer. Brand assets are served from
      # the headlamp-brand ConfigMap mounted on the enbuild-ui pod
      # (templates/enbuild-ui.yaml + templates/headlamp-brand-configmap.yaml).
      # sub_filter match strings are verbatim from the served Headlamp
      # index.html; re-capture + update if the Headlamp image bumps.
      location /headlamp-brand/ {
        alias /usr/share/nginx/headlamp-brand/;
        access_log off;
        add_header Cache-Control "public, max-age=300";
      }

      location /headlamp/ {
        proxy_pass http://{{ .Release.Name }}-headlamp/headlamp/;

        # WebSocket upgrade pass-through (Headlamp pod logs/exec/watch
        # streams). The $connection_upgrade map drops the Connection header
        # for plain requests, keeping upstream keepalive viable.
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # sub_filter can't rewrite a gzipped upstream — force plain HTML.
        proxy_set_header Accept-Encoding "";
        sub_filter_once off;

        sub_filter '<title>Headlamp</title>' '<title>ENBUILD Cluster Console</title>';
        sub_filter '<meta name="description" content="Headlamp: Kubernetes Web UI" />' '<meta name="description" content="ENBUILD Cluster Console" />';

        sub_filter '<link rel="icon" href="/headlamp/favicon.ico" />' '<link rel="icon" href="/headlamp-brand/favicon.ico" />';
        sub_filter '<link rel="apple-touch-icon" sizes="180x180" href="/headlamp/apple-touch-icon.png" />' '<link rel="apple-touch-icon" sizes="180x180" href="/headlamp-brand/favicon.ico" />';
        sub_filter '<link rel="icon" type="image/png" sizes="32x32" href="/headlamp/favicon-32x32.png" />' '<link rel="icon" href="/headlamp-brand/favicon.ico" />';
        sub_filter '<link rel="icon" type="image/png" sizes="16x16" href="/headlamp/favicon-16x16.png" />' '';
        sub_filter '<link rel="mask-icon" href="/headlamp/safari-pinned-tab.svg" color="#5bbad5" />' '';
        sub_filter '<link rel="apple-touch-icon" href="logo192.png" />' '';

        sub_filter '</head>' '<link rel="stylesheet" href="/headlamp-brand/whitelabel.css"><script src="/headlamp-brand/whitelabel.js"></script></head>';
      }

{{- end }}

      # P1 CCM latency P0 (2026-06-11): Vite emits content-hashed filenames
      # under assets/ — a changed file always gets a new URL, so the browser
      # may cache them forever and skip the tunnel round-trip entirely on
      # reloads. Serves the same static root as the /p1-ccm-console/ block
      # below (inherited root); no SPA fallback here — a missing hashed asset
      # must be a hard 404, never index.html. NOTE: add_header at location
      # level replaces inherited add_headers; there are none at server level
      # in this config, so nothing needs replicating.
      # Deliberately NO 'always' flag: without it nginx attaches the header
      # only to 2xx/3xx success codes, so a missing asset's 404 is never
      # cached immutably by browsers (404 is cacheable-by-default per RFC
      # 9110, and a year-long cached 404 on a deterministic Vite hash URL
      # would survive the fixed image being deployed).
      location /p1-ccm-console/assets/ {
        try_files $uri =404;
        add_header Cache-Control "public, max-age=31536000, immutable";
      }

      # The console entrypoint must always revalidate so a new deploy's
      # index.html (pointing at new hashed assets) is picked up immediately.
      # The SPA fallback below internally redirects here, so deep-link
      # responses get this header too.
      location = /p1-ccm-console/index.html {
        add_header Cache-Control "no-cache" always;
      }

      # P1 CCM Console SPA deep-link fallback (B4 nginx fix).
      # Without this block, hard-refreshes / direct URL hits to client-side
      # React Router routes under /p1-ccm-console/* return 404. The in-cluster
      # ConfigMap on vendor13-ib has carried this patch since the prototype's
      # Phase 6; folded into the chart at 0.0.44-pkiprep.0 so helm-upgrade
      # doesn't wipe the operator-applied patch. See playbook.md
      # "ConfigMap patch (rare)" section for the carryover history.
      location /p1-ccm-console/ {
        try_files $uri $uri/ /p1-ccm-console/index.html;
      }
    } # end of server block
