# ENBUILD CLI

```
❯ enbuild bigbang create-haul -h
Create a haul manifest.yaml file given the BigBang version.

Usage:
  enbuild bigbang create-haul [flags]

Flags:
  -v, --bb-version string   Specify the BigBang version (required)
  -h, --help                help for create-haul
```

###
